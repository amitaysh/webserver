cacheHandler = dict(
    key='*****',
    secret='*****',
    memchached_endpoint='*****',
    redis_endpoint='*****',
    repository_endpoint='*****'
)

webserver = dict(
    server_port=8080,
    count_key_name='requests_count'
)

